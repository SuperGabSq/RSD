# Phase 5: time-domain waveform plot. Defends the browser-facing half of the wire
# protocol the plot reads, which is Python and therefore testable; the rendering itself
# has no test framework by design (plan §4).
